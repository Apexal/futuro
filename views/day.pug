extends layout

block content
  #activities
    .row
      .seven.columns
        i.status(v-show='activities.length > 0') {{ activities.length }} total
        ol
          i.status(v-if='activities.length == 0') No activities logged for this day!
          activity(v-else, v-for='a in activities', :activity='a', @remove='removeActivity')
      .five.columns
        include /includes/activityForm
    .row
      #day-agenda(v-show='showAgenda')
        hr
        .calendar
    .row
      hr
      button.right(@click='showAgenda = !showAgenda') {{ showAgenda ? 'Hide' : 'Show' }} Agenda
      a.button(href='/') Back